package mx.edu.unpa.inventory_backend.enums;

/**
 * Campus físicos de la UNPA.
 * El nombre del enum se almacena tal cual en la BD (ENUM en MariaDB).
 * En el frontend se muestra la etiqueta legible (ej. "Loma Bonita").
 */
public enum Campus {
    LOMA_BONITA,
    TUXTEPEC
}
